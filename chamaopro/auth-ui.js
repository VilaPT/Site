(()=>{'use strict';
function ptAuthMessage(text){const t=String(text||'').trim().toLowerCase();if(t.includes('email rate limit exceeded'))return'Foram enviados demasiados emails de confirmação num curto período. Aguarda um pouco e tenta novamente.';if(t.includes('invalid login credentials'))return'Email ou palavra-passe incorretos.';if(t.includes('email not confirmed'))return'Confirma primeiro o teu email através da mensagem que recebeste.';return null}
document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('authForm'),btn=document.getElementById('authSubmit'),msg=document.getElementById('authMsg');if(!form||!btn||!msg)return;let timer=null;const unlock=()=>{if(timer){clearTimeout(timer);timer=null}btn.disabled=false;btn.style.opacity='';btn.style.pointerEvents=''};form.addEventListener('submit',()=>{btn.disabled=true;btn.style.opacity='.72';btn.style.pointerEvents='none';timer=setTimeout(unlock,8000)},true);const observer=new MutationObserver(()=>{const replacement=ptAuthMessage(msg.textContent);if(replacement&&msg.textContent!==replacement){msg.textContent=replacement;msg.className='msg err'}if(msg.textContent.trim())unlock()});observer.observe(msg,{childList:true,subtree:true,characterData:true});});
})();